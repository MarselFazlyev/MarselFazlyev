По первой задаче "СSV - JSON парсер"

1) При реализации статического метода parseCSV столкнулся с проблемой передачи в качестве параметра типа класса во внутреннем методе стратегии strategy.setType(), так как хотелось применить максимально обобщенный метод.
Испрльзовав generic, придумал свою корявенькую передачу класса в качестве параметра Class<T> a = T.class , по другому не знаю,как, если подскажете, буду рад.
2) Я не совсем понял, почему нельзя сделать реализацию записи в JSON строку, передав туда сразу список list  одним аргументом toJson(list) , без указания типа списка в кчестве дополнительного аргумента в методе toJson(list,listTYpe).
У меня все корректно отобразилось.

По второй задаче "XML - JSON парсер"
3) Нельзя ли как то проще, без присваивания полям искомого класса значений  дочерних узлов xml, сразу узел с классом который парсим из xml, связать?

По третьей задаче 
4) Надо сначала рапарсить json файл в строку, потом эту строку преобразовать в объекты ?
5)Почему распарсенный json нужно представить в виде строки? Ведь это HashMap.
